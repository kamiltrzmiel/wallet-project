import styled from 'styled-components';

export const Wrapper = styled.div`
  z-index: 100;
  display: flex;
  align-items: center;
  gap: 40px;
  margin-bottom: 50px;
  margin-top: 60px;
  span {
    font-size: 30px;
    text-transform: capitalize;
  }
`;
